apiVersion: v1
kind: Service
metadata:
  name: kubemq-keda-scaler
  labels:
    app.kubernetes.io/name: kubemq-keda-scaler
spec:
  type: ClusterIP
  ports:
    - port: 9090
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    app.kubernetes.io/name: kubemq-keda-scaler
